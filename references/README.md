# References

This folder contains **Git submodules** that provide shared infrastructure. The submodules are referenced — not copied — so every Hexalith repository builds against the same centrally maintained configuration.

